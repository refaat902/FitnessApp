import 'package:flutter/material.dart';

class AiMeal extends StatelessWidget {
  const AiMeal({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Ai recomendations'),),
    );
  }
}